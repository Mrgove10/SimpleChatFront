export class ChatroomList {
    ServerIP: string;
    ChatroomName: string;

    constructor(serverip: string, chatroomname: string) {
        this.ServerIP = serverip;
        this.ChatroomName = chatroomname;
    }
}
